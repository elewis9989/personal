---
import { type Frontmatter } from "@/pages/writing/index.astro";

interface Props {
  url: string;
  frontmatter: Frontmatter;
}

const { url, frontmatter } = Astro.props;
const { title, excerpt, date, minutesRead } = frontmatter;
---

<div class="space-y-2">
  <a href={url} class="underline underline-offset-8 text-xl">{title}</a>
  <p class="font-light">{excerpt}</p>
  <div class="space-x-4 text-sm font-medium">
    <time
      >🗓️ {
        new Date(date).toLocaleDateString("en-us", {
          year: "numeric",
          month: "short",
          day: "numeric",
        })
      }</time
    ><span>⏳ {minutesRead}</span>
  </div>
</div>
