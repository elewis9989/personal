---
import SpeedInsights from "@vercel/speed-insights/astro";
import "@styles/global.css";
import Head from "@components/Head.astro";
import "@fontsource-variable/inter";
import "@fontsource-variable/noto-nastaliq-urdu";
import "@fontsource-variable/noto-serif";
import Nav from "@components/navigation/Nav.astro";
import { Image } from "astro:assets";
import graphImg from "../images/graph.svg";

interface Props {
  title: string;
  description: string;
  image?: string;
  type?: string;
  date?: Date;
}

const { title, description, image, type, date } = Astro.props;
---

<!doctype html>
<html lang="en">
  <Head
    title={title}
    description={description}
    type={type}
    image={image}
    date={date}
  />
  <body class="relative">
    <SpeedInsights />
    <header class="flex items-center md:pb-8 justify-between">
      <Nav />
      <Image
        src={graphImg}
        alt="Three polygons connected"
        class="md:w-16 w-12 h-auto"
      />
    </header>
    <main class="text-base font-serif pt-4 md:pt-0"><slot /></main>
  </body>
</html>
